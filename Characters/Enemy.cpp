#include "Enemy.h"

void attack(Character& target) {
    //rng a numebr between 1-10
    int atkpwr = rand() % 10 + 1;
};

void defend() {
    //50% to guard
    if (rand() % 2 == 0) {

    };
};

void heal() {
    //25% to heal
    if (rand() %2 == 0){
        
    };
};